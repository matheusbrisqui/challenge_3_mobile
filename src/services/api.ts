/**
 * Placeholder para integração com APIs financeiras.
 * Coloque aqui as funções que chamam a sua API C# (RESTful) para:
 * - autenticar usuários
 * - obter dados de mercado (juros, câmbio)
 * - gerar recomendações de carteira via ML
 *
 * Exemplo (fetch):
 *
 * export async function login(email: string, password: string) {
 *   const res = await fetch('https://sua-api/login', { method: 'POST', body: JSON.stringify({ email, password }) });
 *   return await res.json();
 * }
 *
 * Nota: não coloque chaves sensíveis no app; use backend para manter segredos e para executar modelos de ML.
 */
